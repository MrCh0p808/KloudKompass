# Core runtime dependencies
click>=8.1
rich>=10.9
python-dotenv>=1.0
toml>=0.10
textual>=0.50

# AWS support (optional in pyproject.toml, included here for convenience)
boto3>=1.26.0

# Dev / test dependencies
pytest>=7.0
pytest-cov>=4.0
coverage>=7.0
black>=23.0

# Transitive dependencies pinned for reproducibility
humanize>=4.0
Pygments>=2.19
typing_extensions>=4.0
platformdirs>=4.0
markdown-it-py>=3.0
